# Database Connection Info for Docker Environment

# Users MS (Laravel + PostgreSQL)
USERS_DB_HOST=postgres-users
USERS_DB_PORT=5432
USERS_DB_NAME=users_db
USERS_DB_USER=postgres
USERS_DB_PASS=postgres

# Hotels MS (Spring Boot + PostgreSQL)
HOTELS_DB_HOST=postgres-hotels
HOTELS_DB_PORT=5432
HOTELS_DB_NAME=hotels_db
HOTELS_DB_USER=postgres
HOTELS_DB_PASS=postgres

# Reservations MS (.NET + SQL Server)
RESERVATIONS_DB_HOST=sqlserver-reservations
RESERVATIONS_DB_PORT=1433
RESERVATIONS_DB_NAME=reservations_db
RESERVATIONS_DB_USER=sa
RESERVATIONS_DB_PASS=YourStrong@Passw0rd

# Notifications MS (Spring Boot + MongoDB)
NOTIFICATIONS_DB_HOST=mongodb-notifications
NOTIFICATIONS_DB_PORT=27017
NOTIFICATIONS_DB_NAME=notifications_db
NOTIFICATIONS_DB_USER=admin
NOTIFICATIONS_DB_PASS=admin
